# m2-model

A private modeling tool for invited participants. Access requires credentials.

The page content is AES-256-GCM encrypted at rest; `tools/encrypt_site.py` builds the
gate and payloads from sources kept outside this repository.
